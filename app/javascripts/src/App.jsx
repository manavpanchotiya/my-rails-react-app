import React from "react";
import { Button } from "@/components/ui/button"
const App = props => (
  <div className="bg-black">
  <h1>Hello, Rails with React and Vite!</h1>
  <Button>Button</Button>
  </div>
);

export default App;